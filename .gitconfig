[core]
   editor = nvim
[alias]
   l = !git log --pretty=format:'%C(blue)%h %C(red)%d%C(white)%s - %C(cyan)%cn, %C(green)%cr'
   s = !git status
   p = !git pull origin --ff-only
   ps = !git push
   ac = !git add -A && git commit -m
   ammend = !git commit --ammend -m
